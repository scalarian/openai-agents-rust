# Python Main Parity Research

## Current Surface
- Upstream Python main now foregrounds sandbox agents as a core SDK concept.
- Public surface includes core agents/runtime, tools, handoffs, sessions, tracing, realtime, voice, provider integrations, and a large sandbox ecosystem.

## Recent Changes
- Sandbox Agents landed in a large upstream change set with new docs, examples, and API surface.
- README shifted to present sandbox agents as a first-run concept.
- Main is ahead of the latest tag with additional run-item metadata and fixes.

## Release Signals
- Upstream cut `v0.14.0` and `v0.14.1` around the sandbox release.
- Current `main` is ahead of `v0.14.1`, so mission parity targets current `main`, not the last release tag.

## Recommended Buckets
- runtime/provider parity
- sandbox local/runtime parity
- Docker sandbox and mounts
- hosted sandbox providers
- README/docs/examples parity
- release/publish hardening
